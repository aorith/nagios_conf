define command {
    command_name                    check_snmp_cpu
    command_line                    $USER1$/check_snmp_cpu -H $HOSTADDRESS$ -C $ARG1$
}

define command {
    command_name                    prueba1
    command_line                    $USER1$/prueba1.sh $HOSTADDRESS$
}
